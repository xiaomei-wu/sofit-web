{
  "name": "sofit-web",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "install": "npm install --force",
    "start": "npm run start:backend && npm run start:frontend",
    "start:backend": "cd backend && npm install && npm build && npm start",
    "start:frontend": "cd frontend && npm install --force && npm build && npm start"
  },
  "engines": {
    "node": "18",
    "npm": "8.19.2"
  },
  "private": true
}