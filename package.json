{
  "name": "sofit-web",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "docker-compose -f docker-compose.dev.yml",
    "install": "npm install",
    "start": "npm run start:backend && npm run start:frontend",
    "start:backend": "cd backend && npm install && npm build && npm start",
    "start:frontend": "cd frontend && npm install && npm build && npm start"
  },
  "engines": {
    "node": "18",
    "npm": "8.19.2"
  },
  "private": true,
  "devDependencies": {
    "nx": "16.7.4",
    "nx-cloud": "^16.3.0"
  },
  "resolutions": {
    "@babel/traverse": "7.23.2"
  },
  "dependencies": {
    "typescript": "5.1.3"
  }
}
