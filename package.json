{
  "name": "sofit-web",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "npm run start:backend & npm run start:frontend",
    "start:backend": "cd backend && && npm install && npm run build && node dist/main.js",
    "start:frontend": "cd frontend && npm install --force && npm run build && npm run start"
  },
  "engines": {
    "node": "18",
    "npm": "8.19.2"
  },
  "private": true
}